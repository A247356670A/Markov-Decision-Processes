# example given
# Returns an MDP where each action that satisfies a given predicate has an added cost of 1.

# Returns an MDP where specified action is forbidden (except if no other action is allowed).

# Returns an MDP where a specified action can be performed only once.

# Returns an MDP where a specified action must be performed between any two applications of another specified action.

# Returns an MDP that must agree with a specified automaton.